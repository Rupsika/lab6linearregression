apiVersion: v1
kind: Service
metadata:
  name: linear-regression-service
spec:
  type: NodePort
  selector:
    app: linear-regression
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30008
